{
  "name": "rtb-storm-simulator",
  "description": "Interactive simulations of RTB Fabric connection storms, outbound pools, DNS distribution, and scaling",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.12",
    "vitest": "^3.2.6"
  }
}
